services:
  3cx-zammad-bridge:
    build: .
    container_name: 3cx-zammad-bridge
    restart: always
    volumes:
      - //e/3cx-zammad-bridge/config.yaml:/etc/3cx-zammad-bridge/config.yaml
    networks:
      - zammad_default

networks:
  zammad_default:
    external: true
